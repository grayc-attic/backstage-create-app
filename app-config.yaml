#
# print schema: yarn backstage-cli config:schema
# check schema: yarn backstage-cli config:check
#

organization:
  name: My Company

app:
  title: Scaffolded Backstage App
  baseUrl: http://localhost:3000

backend:
  baseUrl: http://localhost:7007
  listen: 0.0.0.0:7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: http://localhost:3000
    methods: [GET, POST, PUT, DELETE]
    credentials: true
  database:
    client: sqlite3
    connection: ':memory:'
  cache:
    store: memory
    workingDirectory: /tmp/backstage

integrations:
  github:
    - host: github.com
      token: ${GITHUB_TOKEN}

auth:
  environment: production
  providers:
    github:
      production:
        clientId: ${GITHUB_CLIENT_ID}
        clientSecret: ${GITHUB_CLIENT_SECRET}

catalog:
  rules:
    - allow:
        - Group
        - User
        - Component
        - API
        - Domain
        - Resource
        - Location
        - System
        - Template
  locations:
    - type: github-org
      target: https://github.com/grayc-de
    - type: github-discovery
      target: https://github.com/grayc-de

# http://backstage.io/docs/features/techdocs/configuration
techdocs:
  builder: local
  generator:
    runIn: local
  publisher:
    type: local
